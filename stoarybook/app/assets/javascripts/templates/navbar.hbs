<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container-fluid">
      <div class="brand strong">{{#link-to 'about'}}Ember-Rails-Devise{{/link-to}}</div>
      <ul class="nav">
          <li>{{#link-to 'about'}}Help{{/link-to}}</li>
      </ul>
      <div class="btn-group pull-right">
        {{#if Stoarybook.AuthController.isAuthenticated}}
            <button class="btn" {{action logout}}>Logout</button>
        {{else}}
            {{view Stoarybook.MenuItem href="#/login" label="Login" }}
            {{view Stoarybook.MenuItem href="#/registration" label="Register"}}
        {{/if}}
      </div>
    </div>
  </div>
</div>
